version: "3.9"
services:

  db:
      image: mysql:5.7
      container_name: data_base
      environment:
          MYSQL_ROOT_PASSWORD: "RootPassword"
          MYSQL_DATABASE: "app_db"
          MYSQL_USER: "db_user"
          MYSQL_PASSWORD: "AdMiN"
      ports:
        - "3306:3306"
      volumes:
          - /var/lib/mysql
          
  backend:
      image: "zar_aggregator_backend"
      ports:
          - "8000:8000"
          - "443:443"
          - "222:22"
          - "36723:36723"
          - "54982:54982" 
          - "80:80"
      env_file:
          - ../.env
      build:
        context: ../
        dockerfile: backend.dockerfile
        args:
            INSTALL_DEV: false
      depends_on:
          - db # старт после старта контейнера db